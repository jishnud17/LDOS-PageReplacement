# Build artifacts
obj/
bin/
lib/
*.o
*.so

# ML data output: raw CSVs are transient (runs write them here; local
# analysis gunzips them from the tracked .gz archives). Only .gz is tracked.
/ml_dataset.csv
/ml_dataset_*.csv
data/**/ml_dataset_*.csv
tot_gups.txt

# Datasets too large for GitHub's 100MB limit (xsbench 405MB, liblinear
# 305MB gzipped).  Kept locally for analysis; regenerate with the commands
# in results/workload_run_inventory.txt.  Their analysis outputs ARE tracked
# under results/real_workloads/.
data/large_local/

# Editor / OS junk
.DS_Store
*.swp
*.swo
*~
.vscode/
.idea/

# Python bytecode
__pycache__/
*.pyc
